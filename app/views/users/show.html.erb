
<%if current_user %>
  <div class="container">
    <div class="page-header">
<h1>Details</h1>
    </div>
<% if current_user %>
  <span class="label label-primary"> Logged in as <%= current_user.email %></span>
  <%= link_to 'Log Out', logout_path, class: 'btn btn-danger btn-xs' %>
<% else %>
<%end %>
    </div>
    <br>
    <div class="container">
<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

  <%= link_to 'My Articles', user_articles_path(@user), class: 'btn btn-primary btn-xs' %>




<%= link_to 'Home', root_url, class: 'btn btn-info btn-xs' %>
<%= link_to 'Edit Profile', edit_user_path(@user), class: 'btn btn-danger btn-xs'%>
    </div>
<%end %>

